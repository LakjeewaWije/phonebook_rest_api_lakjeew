# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Default path will displays the index file
GET     /                           @com.lakjeew.phonebook.controllers.HomeController.index()
#path used to add users
POST    /user                       @com.lakjeew.phonebook.controllers.UserController.addUser()
#path used to update users
PUT    /user                        @com.lakjeew.phonebook.controllers.UserController.updateUser()
#path used to delete users
DELETE  /user/:idToDelete            @com.lakjeew.phonebook.controllers.UserController.deleteUser(idToDelete:Long)



# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

